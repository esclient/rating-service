spring:
  application:
    name: rating-service
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
  liquibase:
    enabled: false
grpc:
  server:
    port: 9090
management:
  metrics:
